import java.util.Arrays;
import java.util.Scanner;

public class ClosestPair{
  static Point p[];
  
  static class Point{
    int x, y;
    public Point(int x, int y){
      this.x = x;
      this.y = y;
  }
 
public static int div(int SL,int SR){
  int size = SR - SL + 1;
  if (size<=3){
            return bruteForce(SL,SR);
        }
        int mid = (SL + SR)/2;
        int SL_s = div(SL,mid);
        int SR_s = div(mid+1,SR);

        int min_s = Math.min(SL_s, SR_s);


    }
    public static int bruteForce(int SL, int SR){
        int d_min = Integer.Max_VALUE;
        int dist = 
    }
     public static void main (String [] args){
 
}
